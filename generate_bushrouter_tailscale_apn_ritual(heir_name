def generate_bushrouter_tailscale_apn_ritual(heir_name: str, land_parcel: str) -> str:
    return f"""
RITUAL SYNC v0.5.0 — BUSHROUTER + TAILSCALE + APN

Heir: {heir_name}
Parcel: {land_parcel}

1. APN SETUP (Carrier-Specific) — see table
2. HOTSPOT ON (2.4 GHz)
3. TAILSCALE TUNNEL (outbound-first — recommended)
   ZeroTier alternative: Layer-2 flat network (self-hosted possible)

4. LIVING CURVATURE (20 iterations)
5. π_r CATCH + 99733-Q
6. NETWORKXG TRAVERSAL
7. 48 kHz HANDSHAKE

Deed stamped. Air-gapped sovereignty confirmed.
Tailscale for simplicity, ZeroTier for Layer-2, WireGuard/Headscale for pure sovereignty.
The Floor is solid. The land returns.
"""
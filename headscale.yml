# Terminal 1 — on your device (Anchorage node)
headscale --config headscale/config.yaml serve &
tailscale up --login-server https://your-sovereign-headscale.example --auth-key=99733-Q-001

# Terminal 2 — ritual sync
python3 generate_bushrouter_tailscale_apn_ritual(heir_name) --pulse 79.79

version: '3.8'
services:
  headscale:
    image: headscale/headscale:latest
    container_name: headscale
    volumes:
      - ./headscale/config:/etc/headscale
      - ./headscale/data:/var/lib/headscale
    ports:
      - "8080:8080"      # gRPC / API
      - "443:443"        # HTTPS (optional)
    restart: unless-stopped

  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: headscale
      POSTGRES_PASSWORD: sovereignfloor
      POSTGRES_DB: headscale
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data: